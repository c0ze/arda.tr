import siteConfig from "../../config/site.config.json";

export const site = siteConfig.site;
export const pages = siteConfig.pages;
export const sectionIds = siteConfig.sections;
export const themes = siteConfig.themes;

